<?xml version="1.0" encoding="UTF-8"?>
<Flow xmlns="http://soap.sforce.com/2006/04/metadata">
    <apiVersion>65.0</apiVersion>
    <description>Record-triggered flow on Policy Master. When a policy is updated to Status = Cancelled, all of its open claims (New or Under Review) are automatically set to Rejected, because a cancelled policy can no longer pay out. Approved and Paid claims are intentionally left untouched. Runs after save.</description>
    <environments>Default</environments>
    <interviewLabel>Policy Cancelled - Reject Open Claims {!$Flow.CurrentDateTime}</interviewLabel>
    <label>Policy Cancelled - Reject Open Claims</label>
    <processType>AutoLaunchedFlow</processType>
    <recordUpdates>
        <name>Reject_Open_Claims</name>
        <label>Reject Open Claims</label>
        <locationX>176</locationX>
        <locationY>287</locationY>
        <filterLogic>1 AND (2 OR 3)</filterLogic>
        <filters>
            <field>Policy__c</field>
            <operator>EqualTo</operator>
            <value>
                <elementReference>$Record.Id</elementReference>
            </value>
        </filters>
        <filters>
            <field>Status__c</field>
            <operator>EqualTo</operator>
            <value>
                <stringValue>New</stringValue>
            </value>
        </filters>
        <filters>
            <field>Status__c</field>
            <operator>EqualTo</operator>
            <value>
                <stringValue>Under Review</stringValue>
            </value>
        </filters>
        <inputAssignments>
            <field>Status__c</field>
            <value>
                <stringValue>Rejected</stringValue>
            </value>
        </inputAssignments>
        <object>Insurance_Claim__c</object>
    </recordUpdates>
    <start>
        <locationX>50</locationX>
        <locationY>0</locationY>
        <connector>
            <targetReference>Reject_Open_Claims</targetReference>
        </connector>
        <doesRequireRecordChangedToMeetCriteria>true</doesRequireRecordChangedToMeetCriteria>
        <filterLogic>and</filterLogic>
        <filters>
            <field>Status__c</field>
            <operator>EqualTo</operator>
            <value>
                <stringValue>Cancelled</stringValue>
            </value>
        </filters>
        <object>Policy_Master__c</object>
        <recordTriggerType>Update</recordTriggerType>
        <triggerType>RecordAfterSave</triggerType>
    </start>
    <status>Active</status>
</Flow>
