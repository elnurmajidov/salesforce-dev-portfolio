ValidateAddress validator = new ValidateAddress();
validator.validateHomeAddress();

String result1 = PracticeMethods.refineText('too fast too furious ', 'too ');
System.debug(result1);

String result2 = PracticeMethods.refineText('apex is really really simple ', 'really ');
System.debug(result2);